run through each standards. each md file is for a file that make up the table system as a whole

each of the files refer to a file in the folder structure

for example ./columns-defs.ts.md refers to ./policy-columns-defs.ts

start with ./columns-defs.ts.md ./table-store.ts.ms
